import { limitToRange as c } from "../lib/utils.js";
import { addYears as u, addMonths as w } from "../lib/date.js";
function v(e, n) {
  const i = {
    date: e.getDate(),
    viewDate: new Date(e.picker.viewDate),
    viewId: e.picker.currentView.id,
    datepicker: e
  };
  e.element.dispatchEvent(new CustomEvent(n, { detail: i }));
}
function g(e, n) {
  const { minDate: i, maxDate: a } = e.config, { currentView: o, viewDate: r } = e.picker;
  let t;
  switch (o.id) {
    case 0:
      t = w(r, n);
      break;
    case 1:
      t = u(r, n);
      break;
    default:
      t = u(r, n * o.navStep);
  }
  t = c(t, i, a), e.picker.changeFocus(t).render();
}
function m(e) {
  const n = e.picker.currentView.id;
  n !== e.config.maxView && e.picker.changeView(n + 1).render();
}
function D(e) {
  e.config.updateOnBlur ? e.update({ revert: !0 }) : e.refresh("input"), e.hide();
}
export {
  g as goToPrevOrNext,
  m as switchView,
  v as triggerDatepickerEvent,
  D as unfocus
};
//# sourceMappingURL=functions.js.map
